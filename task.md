You task is too implement what project proposal says.
`FRA503-proposal.pdf`
But it's hard to implement directly without plan so you need to plan what to do and research for information first.

My thoughts:
- Use this `robot-proj` conda environment.
- I have downloaded the libero dataset located at `/home/shinawatra/Works/personal/robo-proj/LIBERO/libero/datasets`.
- Start with SmolVLA model here `lerobot/smolvla_libero` or `lerobot/smolvla_base`
- Use Firecrawl's browser feature for scraping web information. (You need this)
- I think you need a `lerobot` for using `smolvla`
- Look at this guide `https://github.com/huggingface/VLAb` for finetuning `smolvla` model
- Do not go out of this directory.

Please create a .md plan guide step by step and information we need.
Each technical implement you need to proof it can be implement like "use the smolvla model directly from transformers model" -> you need to run this with sandbox mode and check that first before set in a plan.